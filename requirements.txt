pypdf>=4.0          # full-text PDF labelling (lazy import; falls back to abstracts without it)
python-docx>=1.0    # .docx ingestion (lazy import; only needed for .docx sources)
psycopg[binary]>=3.1  # Postgres driver for the deployed portal (local dev uses stdlib sqlite)
# Core engine, viewer build, URL/txt/html ingestion, --dry-run, and the local sqlite store
# need NO third-party packages — the above are only for full-text PDF, docx, and Postgres.
