pypdf>=4.0      # PDF ingestion (lazy import; only needed for .pdf sources)
python-docx>=1.0 # .docx ingestion (lazy import; only needed for .docx sources)
# Core engine, viewer build, URL/txt/html ingestion, and --dry-run need NO third-party packages.
